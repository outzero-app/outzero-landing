---
/**
 * "Nuestra historia" — narrative copy alongside a real spot photo.
 */
import type { Locale } from "../i18n/translations";
import { t } from "../i18n/translations";
import { inlineJson } from "../lib/inline-json";
import { fetchLandingSpots } from "../lib/spots";
import PhotoCredit from "./PhotoCredit.astro";

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const strings = t(locale);
const landing = await fetchLandingSpots(locale);
const photo = landing.showcase.story;
const storyPool = landing.rotation.story;
const storyPoolForClient =
  storyPool.length > 0
    ? storyPool
    : photo
      ? [
          {
            id: photo.spotId,
            name: photo.name,
            url: photo.url,
            author: photo.authorHandle,
          },
        ]
      : [];
---

<section id="about" class="story-section" data-story-root aria-labelledby="about-title">
  <div class="oz-story-grid">
    <div>
      <p class="section-kicker">{strings.about_label}</p>

      <h2 id="about-title" class="story-headline" set:html={strings.about_headline_html}></h2>

      <p class="story-copy">{strings.about_paragraph_1}</p>

      <p class="story-copy" set:html={strings.about_paragraph_2_html}></p>
    </div>

    {/* The src is server-rendered so the page still works without JS. This
        section sits well below the fold, so the lazy image has not started
        loading by the time the rotation script swaps it. */}
    {photo && (
      <div class="story-photo-wrap">
        <img src={photo.url} alt="" class="story-photo" data-story-photo loading="lazy" />
        <PhotoCredit image={photo} locale={locale} placement="bottom-left" />
      </div>
    )}
  </div>
</section>

<script type="application/json" data-story-rotation set:html={inlineJson(storyPoolForClient)}></script>

<script>
  import { rotatePhoto } from "../scripts/rotate-photo";

  rotatePhoto("[data-story-root]", "[data-story-rotation]", "[data-story-photo]");
</script>

<style>
  .story-section {
    padding-block: var(--section-padding);
    background: var(--splash);
    border-block: 1px solid color-mix(in srgb, var(--border) 60%, transparent);
  }

  .oz-story-grid {
    display: grid;
    gap: 48px;
    grid-template-columns: 1.15fr 0.85fr;
    align-items: center;
    max-width: 1200px;
    margin: 0 auto;
    padding-inline: clamp(20px, 5vw, 48px);
  }

  @media (max-width: 880px) {
    .oz-story-grid {
      grid-template-columns: 1fr;
    }
  }

  .story-headline {
    margin: 16px 0 0;
    font-size: clamp(20px, 2.4vw, 30px);
    font-weight: 300;
    line-height: 1.5;
    letter-spacing: -0.4px;
    color: var(--text-primary);
  }

  .story-headline :global(em) {
    font-style: normal;
    font-weight: 600;
    color: var(--tag-green);
  }

  .story-copy {
    margin: 22px 0 0;
    font-size: clamp(14px, 1.3vw, 17px);
    font-weight: 300;
    line-height: 1.7;
    color: var(--text-secondary);
  }

  .story-copy + .story-copy {
    margin-top: 14px;
  }

  .story-copy :global(strong) {
    color: var(--text-primary);
    font-weight: 600;
  }

  .story-photo-wrap {
    position: relative;
    display: flex;
  }

  .story-photo {
    width: 100%;
    max-height: 440px;
    aspect-ratio: 4 / 5;
    object-fit: cover;
    border-radius: var(--radius-2xl);
    border: 1px solid var(--border-subtle);
    box-shadow: var(--shadow-card);
  }
</style>
